{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm ci --no-audit --no-fund --prefer-offline",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
